
maxWidth(width)
    @media all and (max-width: width)
        {block}

minWidth(width)
    @media all and (min-width: width)
        {block}

maxHeight(height)
    @media all and (max-height: height)
        {block}

minHeight(height)
    @media all and (min-height: height)
        {block}

rotation(interval = 1s)
    :global {
        animation: rotation interval infinite linear
    }

$blueTextColor = #4B91E5;
$blueColor = #62A4EF
$blueColorHove = #5A96DB
$blueColorActive = #528ACA
$grayColor = #DADCE0
$chatBgColor = #E7EBEE
$white = #ffffff
$black = #000000

$grayBorderColor = #DADCE0
$grayBgColor = #F4F4F5
$grayAvatarBgColor = #e8e8e8;
$grayIconColor = #C5C9CC
$grayTextColor = #717579
$red = #D4483E
